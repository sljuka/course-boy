# Verifying Courses

Users should be able to verify both package integrity and publisher identity before trusting a shared course.

Integrity answers whether the bytes match the expected package contents. Signatures answer whether the package came from a publisher whose public key you trust.

Matko should treat imported content as untrusted until those checks pass.
