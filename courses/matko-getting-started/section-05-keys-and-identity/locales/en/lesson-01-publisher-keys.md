# Publisher Keys

A publisher key pair lets a course maintainer sign package metadata so other users can verify who produced it.

In practice, the private key stays with the maintainer, and the public key is published in course metadata. The signature is created over generated package data such as a manifest of files and hashes.

This gives users a way to distinguish a trusted package from an altered copy.
